type CourseItem @entity {
  id: ID!
  author: CourseAuthor!
  status: ItemStatus!
}

enum ItemStatus {
  Activated
  Deactivated
}

type CourseAuthor @entity {
  id: ID!
  address: Bytes!
}

type PurchasedItem @entity {
  id: ID!
  buyer: Bytes!
}
